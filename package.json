{
  "name": "scraper-server",
  "version": "1.0.0",
  "scripts": {
    "start": "npx playwright install-deps chromium && npx playwright install chromium && node server.js"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "express": "^4.18.2"
  }
}